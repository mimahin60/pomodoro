[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pomodoro"
version = "1.0.0"
description = "A simple GUI Pomodoro timer for Linux"
requires-python = ">=3.9"

[project.scripts]
pomodoro = "main:main"
